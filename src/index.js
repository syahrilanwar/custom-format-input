import CustomFormatInput from "./CustomFormatInput.vue";

const CustomFormatInputVue = {
  install(Vue) {
    Vue.component("custom-format-input-vue", CustomFormatInput);
  },
};

export default CustomFormatInputVue;
